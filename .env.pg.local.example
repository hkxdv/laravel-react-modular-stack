# ====================================================================
# == ENTORNO DE DESARROLLO LOCAL CON POSTGRESQL ==
# ====================================================================
# Este archivo contiene las variables para el desarrollo local con PostgreSQL.
# Se utiliza al ejecutar los scripts de `package.json` con prefijo pg:.

# --- Variables de la Aplicación ---
APP_NAME="Laravel React Modular Stack"
APP_ENV=local
# ¡IMPORTANTE! Genera una clave para tu entorno PostgreSQL y pégala aquí.
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8080
FRONTEND_URL=http://localhost:5173
APP_RUNNING_IN_CONTAINER=false

# --- Configuración de Logging ---
LOG_CHANNEL=daily
LOG_LEVEL=debug
LOG_DEPRECATIONS_CHANNEL=null
LOG_DAYS=7

# --- Configuración de Sesión y CORS ---
SANCTUM_STATEFUL_DOMAINS=localhost:5173,localhost:8080,127.0.0.1:5173,127.0.0.1:8080
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_COOKIE=PROJECT_SESSION
SESSION_DOMAIN=localhost
SESSION_SAME_SITE=lax
SESSION_SECURE_COOKIE=false

# --- Configuración de PostgreSQL local ---
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1 # Host predeterminado para PostgreSQL local
DB_PORT=5432 # Puerto predeterminado para PostgreSQL
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

# --- Configuración de Colas ---
QUEUE_CONNECTION=sync

# --- Variables para el Frontend (Vite) ---
VITE_APP_NAME="${APP_NAME}"
VITE_APP_URL="${APP_URL}"

# --- Configuración para mostrar errores detallados de Laravel en lugar de los personalizados de Inertia ---
SHOW_LARAVEL_ERRORS=true

# --- Configuración para typesense ---
SCOUT_DRIVER=typesense 